"""SOP Agent — author high-quality SOPs sized to how your team actually operates.

Public API:
    from sop_agent.overlay import resolve_overlay, OverlayConfig

CLI entry point:
    sop-agent [onboard|new|validate|list]
"""

__version__ = "0.1.0"
